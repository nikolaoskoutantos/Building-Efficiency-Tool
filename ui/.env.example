# API Configuration
# Base URL for the backend API (no trailing slash)
VITE_API_BASE_URL=http://localhost:8000

# Application Configuration
VITE_APP_NAME=QoE Application
VITE_APP_ENV=development

# API Settings
VITE_API_TIMEOUT=10000

# Debug Mode (true/false)
VITE_DEBUG_MODE=false

# ===========================================
# SECURITY NOTES:
# ===========================================
# ALL VITE_ prefixed variables are PUBLIC and will be bundled into the client-side code.
# Do NOT put sensitive information like API keys, secrets, or tokens here.
# These variables are safe to expose:
# - API base URLs (they're visible in network requests anyway)
# - Application names and non-sensitive configuration
# - Feature flags and UI settings
# 
# For sensitive configuration, use a secure config endpoint from your backend API.