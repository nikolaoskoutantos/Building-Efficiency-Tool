version: '3.8'
services:
  ui:
    image: node:18
    working_dir: /app
    volumes:
      - ./:/app
    command: sh -c "npm install --legacy-peer-deps && npm run dev -- --host"
    ports:
      - "5173:3000"
    env_file:
      - .env
