JENKINS_ADMIN_ID=admin
JENKINS_ADMIN_PASSWORD=change_me_now
JENKINS_URL=http://localhost:18081/
JENKINS_HOST=jenkins.nkoutantos.com

# Local compose:
#   docker compose up -d
#   UI: http://localhost:18081
#
# Production/Traefik compose:
#   docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d
